Сборка:
    Требуются установленные maven и jdk. перейдите в папку с pom.xml и запустите в командной строке
    mvn package
    Скомпилированный и собраный файл будет находится в ./target/PropertiesToHTML.jar

Использование:

java -jar PropertiesToHTML.jar

    Для корректного отображения сгенерированного файла в браузере требуется интернет подключение.

    Веб приложение будет находиться по адресу http://locallhost:8080

Примечания:
    .properties файл должен быть в кодировке UTF-8

    В донном варианте используются файлы part1.properties и part2.properties

    если в свойстве должно быть больше одного значения, то они разделяются символом \n
    Поля поддерживающие более одного значения:
    phone, email, goals, education, additionalEducation, skills, codeSample.
    Поля поддерживающие ключ, значение значения:
    workExperience